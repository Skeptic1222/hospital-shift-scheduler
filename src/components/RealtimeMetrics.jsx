import React from 'react';
import { Typography } from '@mui/material';

const RealtimeMetrics = () => (
  <Typography color="text.secondary">Live metrics stream appears here.</Typography>
);

export default RealtimeMetrics;

